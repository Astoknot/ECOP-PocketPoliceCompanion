<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bellatrix.aditi.e_cop">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".User.UserActivity" />
        <activity android:name=".Cop.CopActivity" />
        <activity android:name=".Court.CourtActivity" />
        <activity android:name=".User.FileComplaintActivity" />
        <activity android:name=".ViewCasesActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".Cop.LodgeFIRActivity" />
        <activity
            android:name=".User.SignUpAsUserActivity"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity android:name=".Cop.SignUpAsCopActivity" />
        <activity
            android:name=".CaseDescriptionActivity"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity android:name=".Court.SignUpAsCourtActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".ViewComplaintsActivity" />
        <activity android:name=".ViewUpdatesActivity" />
        <activity android:name=".Cop.OpenCaseActivity" />
        <activity android:name=".Cop.UpdateCaseActivity" />
        <activity android:name=".Cop.RejectComplaintActivity" />
        <activity android:name=".ViewFIRActivity" />
        <activity android:name=".SearchActivity"></activity>
    </application>

</manifest>